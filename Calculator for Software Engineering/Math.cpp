#include "Math.h"

int Math::Add(float first, float second)
{
	int answer = 0;

	return answer = first + second;
}

int  Math::Subtract(float first, float second)
{
	int answer = 0;

	return answer = first - second;
}

int  Math::Multiply(float first, float second)
{
	int answer = 0;

	return answer = first * second;
}

int  Math::Divide(float first, float second)
{
	int answer = 0;

	return answer = first / second;
}